name: Dependency Submission Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Dependency submission disabled
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Dependency submission is disabled for now so APK builds are not blocked by optional dependency graph jobs."
