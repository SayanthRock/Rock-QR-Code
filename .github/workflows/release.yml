name: Old Release Workflow Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Use Manual APK and AAB Package
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Use the Manual APK and AAB Package workflow. The old release workflow was disabled because the repository Gradle wrapper jar is corrupt."
